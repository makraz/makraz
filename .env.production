# Turnstile SITE key — public by design (it is served inside the contact page HTML).
# Committed so production builds work without extra CI configuration.
# The matching SECRET key is a Worker secret and must never live here.
PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAAD8wN9fzXdF-QKL7
